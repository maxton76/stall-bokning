rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Helper functions
    function isAuthenticated() {
      return request.auth != null;
    }

    function isOwner(userId) {
      return isAuthenticated() && request.auth.uid == userId;
    }

    function getUserData() {
      return get(/databases/$(database)/documents/users/$(request.auth.uid)).data;
    }

    function userExists() {
      return exists(/databases/$(database)/documents/users/$(request.auth.uid));
    }

    function hasRole(role) {
      return isAuthenticated() && userExists() && getUserData().role == role;
    }

    function isSystemAdmin() {
      return hasRole('system_admin');
    }

    function isStableOwner() {
      return hasRole('stable_owner');
    }

    function isStableGuest() {
      return hasRole('stable_guest');
    }

    function belongsToStable(stableId) {
      return isAuthenticated() && userExists() && getUserData().stableId == stableId;
    }

    // Users collection
    match /users/{userId} {
      allow read: if isAuthenticated() && (
        isOwner(userId) ||
        isSystemAdmin() ||
        belongsToStable(resource.data.stableId)
      );
      allow create: if isAuthenticated();
      allow update: if isOwner(userId) || isSystemAdmin();
      allow delete: if isSystemAdmin();
    }

    // Stables collection
    match /stables/{stableId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated();
      allow update: if isAuthenticated();
      allow delete: if isAuthenticated();
    }

    // Shift Types collection
    match /shiftTypes/{shiftTypeId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated();
      allow update: if isAuthenticated();
      allow delete: if isAuthenticated();
    }

    // Schedules collection
    match /schedules/{scheduleId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated();
      allow update: if isAuthenticated();
      allow delete: if isAuthenticated();
    }

    // Shifts collection
    match /shifts/{shiftId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated();
      allow update: if isAuthenticated();
      allow delete: if isAuthenticated();
    }

    // Bookings collection
    match /bookings/{bookingId} {
      allow read: if isAuthenticated() && (
        belongsToStable(resource.data.stableId) ||
        isSystemAdmin()
      );
      allow create: if isAuthenticated() && belongsToStable(request.resource.data.stableId);
      allow update: if isAuthenticated() && (
        isOwner(resource.data.userId) ||
        isStableOwner() ||
        isSystemAdmin()
      );
      allow delete: if isAuthenticated() && (
        isOwner(resource.data.userId) ||
        isStableOwner() ||
        isSystemAdmin()
      );
    }

    // Notifications collection
    match /notifications/{notificationId} {
      allow read: if isAuthenticated() && isOwner(resource.data.userId);
      allow create: if isSystemAdmin();
      allow update: if isAuthenticated() && isOwner(resource.data.userId);
      allow delete: if isAuthenticated() && isOwner(resource.data.userId);
    }

    // Invites collection
    match /invites/{inviteId} {
      allow read: if isAuthenticated();
      allow create: if isAuthenticated();
      allow update: if isAuthenticated();
      allow delete: if isAuthenticated();
    }

    // Default deny all
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
