# Google Cloud Provider Configuration
# This file is symlinked or copied to each environment

provider "google" {
  project = var.project_id
  region  = var.region
}

provider "google-beta" {
  project = var.project_id
  region  = var.region
}
